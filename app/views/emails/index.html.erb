<div class="container">
    <br>
    <div class="row">
        <div class="col-sm">
            <button type="button" class="btn btn-primary btn-lg"><%= link_to "Recevoir un email", emails_path, class: "btn btn-primary", remote: true, method: :post%></button>
            <h3>Liste des emails</h3>
            <ul class="list-group list-group-flush" id="emails-list">
                <% @emails.each do |email|  %>   
                   <a class="remove" href="<%= email_path(email.id)%>" data-remote="true"><%=email.object%></a>
                   </li>
                <% end %>
            </ul>
        </div>
        <div class="col-sm">
        <br><br>
        <h3 id="email-show">Email à afficher</h3>
            <ul class="list-group list-group-flush">
            </ul>
    </div>
    </div>
</div>
